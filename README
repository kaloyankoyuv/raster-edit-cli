Simple raster image editor with command-line interface.

Documentation:

  Open docs/html/index.html to read documentation.

Compilation and running:

  Compile the program by running "make" in the project's root. This
  will create a directory named "build" where the binary is placed.

  To delete this directory and the binary run "make clean" in the
  project's root.

Github repo:
https://github.com/kaloyankoyuv/raster-edit-cli
